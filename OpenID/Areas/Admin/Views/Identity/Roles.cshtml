@using OpenID.Dtos.Common
@using OpenID.Constants
@model OpenID.Dtos.Identity.Interfaces.IRolesDto

@{
    ViewBag.Title = "roles";
    Layout = "_Layout";
}

<div class="row">
    <div class="col-12 mb-3 text-center mt-1">
        <h2><span class="oi oi-lock-locked" title="Roles" aria-hidden="true"></span>Title</h2>
    </div>
</div>

<div class="row">
    <div class="col-12">
        <a asp-area="@CommonConsts.AdminUIArea" asp-action="Role" class="btn btn-outline-primary mb-3 mt-3"><span class="oi oi-plus"></span>AddRole</a>
    </div>
</div>

<div class="row">
    <div class="col-12">
        @await Html.PartialAsync("Common/Search", new Search { Action = "Roles", Controller = "Identity" })
    </div>
</div>

<div class="row">
    <div class="col-12">
        <div class="table-responsive">
            <table class="table table-striped">
                <thead>
                    <tr>
                        <th></th>
                        <th>TableName</th>
                        <th></th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var role in Model.Roles)
                    {
                        <tr>
                            <th>
                                <a asp-area="@CommonConsts.AdminUIArea" class="btn btn-primary" asp-action="Role" asp-route-id="@role.Id">Edit</a>
                                <a asp-area="@CommonConsts.AdminUIArea" class="btn btn-primary" asp-action="RoleUsers" asp-route-roleId="@role.Id">Users</a>
                            </th>
                            <td>@role.Name</td>
                            <td>
                                <a asp-area="@CommonConsts.AdminUIArea" class="btn btn-danger" asp-action="RoleDelete" asp-route-id="@role.Id"><span class="oi oi-x"></span></a>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>

<div class="row">
    <div class="col-12">
        @await Html.PartialAsync("Common/Pager", new Pager { Action = "Roles", PageSize = Model.PageSize, TotalCount = Model.TotalCount })
    </div>
</div>